# ============================================================================
# FTRY SALON SAAS - ENVIRONMENT VARIABLES
# ============================================================================
#
# This is a template file. Copy this to .env and update with your values:
#   cp .env.example .env
#
# IMPORTANT: .env is gitignored and should NEVER be committed to version control
# ============================================================================

# Database Configuration (PostgreSQL)
POSTGRES_USER=ftry
POSTGRES_PASSWORD=ftry
POSTGRES_DB=ftry
POSTGRES_PORT=5432

# Database Connection String (used by NestJS backend)
# Connection pooling: connection_limit controls max connections, pool_timeout in seconds
# statement_timeout in milliseconds prevents long-running queries from blocking
DATABASE_URL=postgresql://ftry:ftry@localhost:5432/ftry?connection_limit=10&pool_timeout=20&statement_timeout=30000

# pgAdmin Configuration (optional - uncomment pgadmin service in docker-compose.yml)
# PGADMIN_EMAIL=admin@ftry.local
# PGADMIN_PASSWORD=admin
# PGADMIN_PORT=5050

# Application Configuration
NODE_ENV=development
PORT=3001

# Frontend Configuration
FRONTEND_URL=http://localhost:3000
VITE_API_URL=http://localhost:3001/api/v1

# JWT/Auth Configuration
# IMPORTANT: Change JWT_SECRET in production to a secure random string
JWT_SECRET=dev-secret-key-change-in-production-8a7f6e5d4c3b2a1
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=7d

# Demo Data Configuration (Development Only)
# Password used for demo users created by prisma/seed.ts
# Default: DevPassword123!@# (meets 8-char minimum + complexity requirements)
# IMPORTANT: Never use this in production - demo users should not exist in production
DEMO_PASSWORD=DevPassword123!@#
